console.log("Time to get starting");

const add = (n1: number, n2: number, isTrue: boolean) => {
  if (isTrue) {
    const result = n1 + n2;
    console.log(result);
  }
};

const inputValue = 3;
const num = 2;
const isTrue = true;

add(inputValue, num, isTrue);

const b = 2;
